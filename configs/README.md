# Configurations

This directory contains configuration files for training, model architectures, and experiments.

## Structure

- `model_config.yaml` - Model architecture configurations
- `training_config.yaml` - Training hyperparameters and settings
- `data_config.yaml` - Data processing and loading configurations

## Usage

Configuration files use YAML format and can be loaded using the `pytorch_transformer.utils.config` module.
