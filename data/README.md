# Data

This directory contains datasets used for training and evaluation.

## Structure

- `raw/` - Original, immutable data dumps
- `processed/` - Cleaned, transformed data ready for training
- `external/` - Data from third-party sources

## Note

Large datasets should not be committed to version control. Use `.gitignore` to exclude them or store references to cloud storage locations.
